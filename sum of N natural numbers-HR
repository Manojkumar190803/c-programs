#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int i,x,sum;
    scanf("%d",&x);
    for(i=1;i<=x;i++)
    {
        sum=i+sum;
    }
    printf("%d",sum);
    return 0;
}

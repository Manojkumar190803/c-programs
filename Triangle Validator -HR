#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int x,y,z;
    scanf("%d%d%d",&x,&y,&z);
    if(x+y>z && x+z>y && y+z>x )
    {
        printf("Yes\n");
    }
    else
    {
      printf("No\n");
    }
    return 0;
}

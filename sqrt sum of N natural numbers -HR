#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int i,x,sum=0;
    scanf("%d",&x);
    for(i=1;i<=x;i++)
    {
        sum=sum+(i*i);
    }
    printf("%d",sum);
    return 0;
}

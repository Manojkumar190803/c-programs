#include <stdio.h>
int main()
{
    int r;scanf("%d",&r);
    int n=1;
    for (int i=0;i<r;i++) {
        for (int j=0; j<=i;j++) {
            printf("%d ",n++);
        }
        printf("\n");
    }
    return 0;
}
